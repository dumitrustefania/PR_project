{
    "name": "gym-attendance-app",
    "engines": {
      "node": "16.x"
    },
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "heroku-prebuild": "cd gym-attendance-frontend && npm install && npm run build && cd .."
    },
    "dependencies": {
      "npm": "^8.5.0"
    }
  }
  